<nav class="navbar navbar-default navbar-fixed-top" role="navigation">

	<div class="container-fluid">
		<!-- Brand and toggle get grouped for better mobile display -->
		<div class="navbar-header">
			<a class="navbar-brand" href="#"><img src="{{ asset('elearnIcon.png')}}" alt="" /> {{ app_name }}</a>
		</div>

		<!-- Collect the nav links, forms, and other content for toggling -->
		<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
			<ul class="nav navbar-nav navbar-right">
				{% if is_granted('ROLE_ADMIN') %}
					<li class="active active-admin"><a href="{{ path('admin_cursos') }}"><span class="glyphicon glyphicon-cog" aria-hidden="true"></span> Admin</a></li>
				{% endif %}
				<li class="dropdown">
					<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">{{ app.user.username }}  <span class="caret"></span></a>
					<ul class="dropdown-menu" role="menu">
						<li><a href="{{ path('front_perfil')}}"><span class="glyphicon glyphicon-user" aria-hidden="true"> Perfil</a></li>
						<li class="divider"></li>
						<li><a href="{{ path('logout') }}"><span class="glyphicon glyphicon-off" aria-hidden="true"> Cerrar</a></li>
					</ul>
				</li>
			</ul>
		</div><!-- /.navbar-collapse -->
	</div><!-- /.container-fluid -->
</nav>
