{% extends '::accesos.html.twig'%}

{% block body %}
  <div class="alert alert-success" role="alert">
    <strong>
      Escriba el nuevo password para su cuenta
    </strong>
  </div>

  <form class="form" action="" method="post" novalidate="novalidate">
    {{ form_widget(form) }}
    <div class="btn-group">
      <button type="submit" class="btn btn-default">
        Enviar
      </button>
      <a href="{{ path('login') }}" type="submit" class="btn btn-warning">
        Login
      </a>
    </div>
  </form>
{% endblock %}
