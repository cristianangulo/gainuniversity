{% extends '::accesos.html.twig'%}

{% block body %}
  
  <div class="alert alert-success" role="alert">
    <strong>
      Ingresa tu dirección de correo para restablecer tu contraseña. <br />
      Puede que tengas que ver en tu carpeta de spam o desbloquear <br />
      no-reply@gainuniversity.com.
    </strong>
  </div>
    <form class="form" action="" method="post" novalidate="novalidate">
        {{ form_widget(form) }}
        <div class="btn-group">
          <button type="submit" class="btn btn-default">Enviar</button>
          <a type="button" class="btn btn-warning" href="{{ path('login') }}">Login</a>
        </div>
    </form>
{% endblock %}
