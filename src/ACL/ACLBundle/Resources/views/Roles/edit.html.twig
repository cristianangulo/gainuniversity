{% extends '::admin.html.twig' %}

{% block body -%}
    <div class="page-header">
      <h1>Perfil del role</h1>
    </div>
    <ul class="record_actions">
      <li>
        <a href="{{ path('roles') }}">
          Regresar
        </a>
        |
        <a href="{{ path('roles_new') }}">
          Crear role
        </a>
      </li>
    </ul>
    {% form_theme edit_form 'ElearnBundle:Form:admin-form.html.twig' %}

    {{ form(edit_form) }}

    <ul class="record_actions">
      <li>
        <a href="{{ path('roles') }}">
            Regresar
        </a>
      </li>
      <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock %}
