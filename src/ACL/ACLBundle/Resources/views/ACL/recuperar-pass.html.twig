{% extends '::accesos.html.twig'%}

{% block body %}
  <p class="lead">
    Escriba el nuevo password para su cuenta
  </p>
  {% form_theme form 'ElearnBundle:Form:admin-form.html.twig' %}
  {{ form(form) }}
  <p>
    <a type="button" class="btn btn-link">Registro</a> |
    <strong>
      <a href="{{ path('login') }}">
        Login
      </a>
    </strong>
  </p>
{% endblock %}
