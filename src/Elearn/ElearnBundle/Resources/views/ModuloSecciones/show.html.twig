{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ModuloSecciones</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Posicion</th>
                <td>{{ entity.posicion }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_modulos_secciones') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_modulos_secciones_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
